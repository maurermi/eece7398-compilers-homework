#include <stdio.h>
#include <math.h>

int main() {
    float angle = 0.5;
    float sin_val = sin(angle);
    float cos_val = cos(angle);
    printf("Sine: %f, Cosine: %f\n", sin_val, cos_val);
    return 0;
}
